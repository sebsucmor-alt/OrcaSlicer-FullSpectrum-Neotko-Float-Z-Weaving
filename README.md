
<h1> <p "font-size:200px;"> Full Spectrum</p> </h1>

### A Snapmaker Orca Fork with Mixed-Color Filament Support

[![Build all](https://github.com/Snapmaker/OrcaSlicer/actions/workflows/build_all.yml/badge.svg?branch=main)](https://github.com/Snapmaker/OrcaSlicer/actions/workflows/build_all.yml)

---

## ☕ Support Development

If you find this fun or interesting! Give RatDoux a Coffee for his work

<a href="https://www.buymeacoffee.com/ratdoux" target="_blank"><img src="https://cdn.buymeacoffee.com/buttons/v2/default-yellow.png" alt="Buy Me A Coffee" style="height: 60px !important;width: 217px !important;" ></a>

---

## ⚠️ **IMPORTANT DISCLAIMER** ⚠️

THIS IS A 0.93 Quite and Dirty fork, with my Beta Neotko Weaving Idea and Specially a Link system to allow free Z objects without needing to make 'parts'. Also allows to use different layer height for the prime tower.

All info documentation and changes listed - Update v43

SO MUCH STUFF CHANGED

# OrcaSlicer FullSpectrum — Neotko Fork Technical Reference

**Fork:** Snapmaker FullSpectrum  
**Base:** OrcaSlicer 0.9.3 (BBS/Bambu fork)  
**Last updated:** 2026-03-13 (session 9)  
**Current ZIP:** `OrcaSlicer-FullSpectrum-patches-18.zip`  
**Authors:** Neotko + Claude (Anthropic)

---

## Purpose of this document

This document serves two purposes:

1. **Resume development from scratch** — it contains enough technical context that a new session can continue without having participated in previous ones.
2. **Reference for other builders** — it explains the reasoning behind each technical decision, not just what changed but why.

---

## Project architecture

OrcaSlicer FullSpectrum is a fork of Bambu Lab's fork of OrcaSlicer (version 0.9.3). The Snapmaker repository already includes some of its own patches. This patch set applies on top of that baseline state.

### Modified files

```
src/
├── libslic3r/                         ← Slicing engine (no UI)
│   ├── PrintConfig.hpp / .cpp         ← All parameter definitions
│   ├── PrintObject.cpp                ← Object preparation logic
│   ├── GCode.cpp                      ← GCode generation (the heart)
│   ├── Preset.cpp                     ← Preset system
│   ├── Print.cpp                      ← Pre-existing Snapmaker patches
│   ├── Model.hpp / .cpp               ← Scene object model
│   ├── Fill/FillBase.cpp              ← Fill base logic
│   └── Format/3mf.cpp                 ← 3MF file read/write
└── slic3r/GUI/                        ← GUI (wxWidgets)
    ├── Tab.cpp                        ← Parameter tabs
    ├── Plater.hpp / .cpp              ← Main window, AUI layout
    ├── GLCanvas3D.hpp / .cpp          ← OpenGL 3D canvas
    ├── GUI_ObjectList.hpp / .cpp      ← Object list panel
    ├── Selection.hpp / .cpp           ← Selection system
    └── ParamsPanel.hpp / .cpp         ← Process parameter panel
```

### OrcaSlicer's configuration system

Understanding how the configuration system works is critical for adding new parameters:

- **`PrintConfigDef`** — global database of all parameters (name, type, tooltip, etc.). Registered in `PrintConfig.cpp` inside `init_fff_params()`.
- **`PrintObjectConfig`**, **`PrintRegionConfig`**, **`PrintConfig`**, **`FullPrintConfig`** — static structs generated by Boost.PP macros in `PrintConfig.hpp`. Every key declared in a macro **must** have a matching entry in `PrintConfigDef` or the application crashes on startup with "Unhandled unknown exception" immediately after logging "Initializing StaticPrintConfigs".
- **`StaticPrintConfig::initialize_cache()`** — validates at runtime that every key declared in the struct macro exists in `PrintConfigDef`. If it doesn't, it throws an untyped exception that OrcaSlicer catches as a crash.
- **`Preset.cpp::s_Preset_print_options`** — list of all key strings saved in a print preset. Omitting a key here won't crash anything, but the key won't be persisted in presets.

**Golden rule when adding a new parameter:**
1. Add the key to the appropriate struct in `PrintConfig.hpp` (macro `PRINT_CONFIG_CLASS_DEFINE`)
2. Register it in `PrintConfig.cpp::init_fff_params()` with `this->add("name", type)`
3. Add it to `s_Preset_print_options` in `Preset.cpp`
4. Add the UI in `Tab.cpp`

---

## Feature 3 — Bridge infill control (`disable_bridge_infill`)

### What it does
Allows disabling bridge infill, either fully or partially. Useful when the model geometry generates unnecessary bridges that worsen top surface quality.

### `DisableBridgeInfill` enum
- `None` — default behavior, normal bridges
- `Partial` — disables only part of the bridge infill
- `Full` — disables bridge infill completely

### Files affected
`PrintConfig.hpp/cpp`, `PrintObject.cpp`, `GCode.cpp`, `Preset.cpp`, `Tab.cpp`

---

## Feature 4 — Top/Bottom surface density controls in UI

### What it does
Exposes top and bottom surface density controls in the UI that existed in the engine but had no accessible control in OrcaSlicer 0.9.3's interface.

### Files affected
`PrintConfig.hpp/cpp`, `Tab.cpp`

---

## Features 5 and 6 — Neotko Neoweaving

This is the most complex and technically interesting feature in the build.

### Context and naming

Neoweaving was invented by Neotko (the same person who invented Ironing). It was previously called "Interlayer Sanding" in the code — a name that caused confusion with the Ironing function. It was renamed to **Neoweaving** in all user-visible labels and keys.

The internal keys remain `interlayer_neoweave_*` and `infill_neoweave_*`.

### The core idea

When printing surface layers with a monotonic pattern, adjacent lines within the same layer touch at their sides, but there's a vertical gap between layers (the layer height). Neoweaving makes lines from layer N physically nestle into the gaps of layer N-1, creating a mechanical interlock that:

- Increases resistance to delamination
- Improves surface cohesion
- Can improve visual finish with certain materials

There are two variants of the same concept:

- **Feature 5 — Surface Neoweaving:** acts on `erTopSolidInfill` (the last layer of the top surface)
- **Feature 6 — Infill Neoweaving:** acts on `erInternalInfill` (sparse internal fill)

### Configuration parameters

**Quality > Neotko Neoweaving** (top surface, Feature 5):

| Parameter | Internal key | Default | Description |
|-----------|-------------|---------|-------------|
| Enable | `interlayer_neoweave_enabled` | false | Master switch |
| Mode | `interlayer_neoweave_mode` | Wave | Wave or Linear |
| Z amplitude | `interlayer_neoweave_amplitude` | 0.10 mm | How far the nozzle moves up/down |
| Period | `interlayer_neoweave_period` | 0 (auto) | Cycle length; 0 = use line width |
| Max Z speed | `interlayer_neoweave_max_z_speed` | 20 mm/s | Z axis speed limit |
| Min line length | `interlayer_neoweave_min_length` | 3.0 mm | Shorter lines are not neoweaved |

**Strength > Neotko Infill Neoweaving** (sparse infill, Feature 6):

| Parameter | Internal key | Default | Description |
|-----------|-------------|---------|-------------|
| Per-object override | `infill_neoweave_enabled` | Inherit | Tristate (see below) |
| Z amplitude | `infill_neoweave_amplitude` | 0.10 mm | |
| Period | `infill_neoweave_period` | 0 (auto) | |
| Max Z speed | `infill_neoweave_max_z_speed` | 20 mm/s | |

**Why `infill_neoweave_enabled` is a tristate enum, not a bool:**  
If it were a `bool`, a `false` value would be indistinguishable from "the user hasn't configured anything" (no override). With the tristate `InfillNeoweaveOverride::Inherit/Enable/Disable`, per-object override can force activation or deactivation regardless of the global preset setting.

### Wave mode — Continuous wave ✅ Validated on hardware

The nozzle follows a **continuous triangular wave** along the entire path. The path is subdivided into micro-segments (minimum 8 per period) so the wave is smooth.

**Z offset calculation for each micro-segment:**

```
period = interlayer_neoweave_period  (or line_width if 0)
phase  = fmod(distance_traveled / period, 1.0) × 4   ← value 0..4

if phase < 1:  z_off = amplitude × phase           ← rising
if phase < 3:  z_off = amplitude × (2 - phase)     ← falling
else:          z_off = amplitude × (phase - 4)     ← rising again
```

The nozzle traces peaks and valleys continuously while extruding.

**XY speed cap (Wave mode):**  
For the Z axis to follow the wave without losing steps, XY speed is globally capped:

```
XY_speed_max = max_z_speed × period / (4 × amplitude)
```

This ensures the actual vertical speed never exceeds `max_z_speed`. At the end of the path, the original speed is restored.

**Important note — Wave mode needs improvement.** Known issues:

1. **Global XY speed cap:** The speed limiter applies to the entire path, unnecessarily slowing down long straight sections where Z movement is minimal. It should be calculated per micro-segment based on the actual Z angle needed at that point.
2. **Triangular vs sinusoidal wave:** The triangular wave has abrupt direction changes at peaks/valleys that can cause ringing. A sinusoidal or quadratic wave would be mechanically smoother.
3. **Pressure advance coordination:** Rapid Z changes during extrusion should coordinate with pressure advance to avoid over/under-extrusion at the wave vertices.
4. **Verbose GCode:** Subdividing into micro-segments generates very long GCode for large paths. Consider using G5 (Bézier) or G2/G3 with helical Z movement if the printer supports it.
5. **CoreXZ/CoreXZY only:** On printers with a Z-moving bed (classic Cartesians), moving Z while extruding is imprecise. The kinematic type should be detected and Wave mode automatically disabled if there's no CoreXZ.

### Linear mode — Per-line flat Z ⚠️ Implemented, awaiting hardware test

In Linear mode, **each line is printed entirely flat at a fixed Z offset** — the nozzle does not move in Z during extrusion. The height change happens in the travel between lines (pure Z move, no extrusion), not during the print move itself.

**Why this architecture:**  
On many printers (especially Cartesian), moving Z while extruding creates artifacts because the Z axis is slow or has too much inertia. With Linear mode, Z only moves during travels (no filament moving), and extrusion is always perfectly flat.

**How the zigzag between lines works:**

```
Even layers (m_layer_index % 2 == 0):
   Line 0 → Z = nominal + 0      (at nominal layer Z)
   Line 1 → Z = nominal + A      (raised A mm)
   Line 2 → Z = nominal + 0
   Line 3 → Z = nominal + A
   ...

Odd layers (m_layer_index % 2 == 1):
   Line 0 → Z = nominal + A      (starts raised)
   Line 1 → Z = nominal + 0
   Line 2 → Z = nominal + A
   ...
```

**Design change in session 7 (from ±A to 0/+A):**  
The first implementation used `nominal - A` and `nominal + A` (symmetric around nominal Z). It was changed to `nominal + 0` and `nominal + A` because with ±A, when two objects on the same bed share Z, the negative travels of one object can visually intersect with the positive lines of another, creating an interference pattern (moiré effect). With 0/+A, no lines go below the layer's nominal Z, eliminating the collision.

**Exact per-line sequence:**
1. `travel_to_z(target_z)` — pure Z move, no extrusion
2. `extrude_to_xy(end_point, dE)` — flat extrusion at that Z

**Inter-layer coordination (the heart of the system):**  
The parity of the layer index (`m_layer_index % 2`) inverts the zigzag phase. Where layer N places a line at `+A`, layer N+1 places its line at `+0` — exactly where the previous layer's gap was. Lines from consecutive layers interdigitate in Z.

```cpp
// GCode.cpp — parity logic
const bool layer_flip   = (m_layer_index % 2 != 0);
const bool line_is_even = (weave_line_idx % 2 == 0);

// XOR: if layer and line have the same parity → positive z_off
const double z_off = ((line_is_even != layer_flip)
    ? weave_amplitude   // raised line
    : 0.0);             // line at nominal Z

gcode += m_writer.travel_to_z(m_nominal_z + z_off, "Neoweaving: line Z");
gcode += m_writer.extrude_to_xy(point_to_gcode(line.b), dE, "Neoweaving linear");
```

**Critical bug fixed — arc fitting bypass (session 7):**  
All neoweaving code was inside an `if (!enable_arc_fitting || ...)` block. With arc fitting enabled (the normal case in OrcaSlicer), that block **never executed**. The only Z movement appearing in the GCode was OrcaSlicer's anti-scratch lift, not neoweave.

Fix: compute `any_neoweave` **before** the if-block and add it as a condition:

```cpp
const bool any_neoweave = weave_active_pre || infill_weave_active_pre;
if (!m_config.enable_arc_fitting
    || path.polyline.fitting_result.empty()
    || m_config.spiral_mode
    || sloped != nullptr
    || any_neoweave)      // ← forces G1 branch when neoweave is active
{
    // ... neoweaving code
}
```

**Critical bug fixed — parity bug with monotonic fill (session 7):**  
Monotonic fill is one connected polyline, not individual lines. The short connector segments between parallel lines also incremented `weave_line_idx`, inverting the parity of all long lines (all ended up at the same Z instead of alternating).

Fix: automatic filter with `max(user_min_length, 2 × line_width)`. Connectors (always < 1 line_width) are always filtered even if the user sets `min_length = 0`.

**Z restoration after each path:**
```
travel_to_z(nominal_z)    // return to exact layer Z
set_speed(F)              // restore speed (Wave mode only)
```

### erSolidInfill support in Linear mode

In session 7, `weave_active` was extended to include `erSolidInfill` (the solid infill layer immediately below the top surface) when mode is Linear. This lets that layer also participate in Z interdigitation, improving adhesion between solid infill and the surface.

**Note:** The correct enum in OrcaSlicer for internal solid infill is `erSolidInfill`, not `erInternalSolidInfill` (which does not exist).

### Solid infill angle lock for Linear mode

For Z interdigitation to work correctly, the solid infill layer below the top surface must have the same fill angle as the top surface. OrcaSlicer by default rotates the angle each layer.

**Solution:** In `FillBase.cpp`, when neoweave Linear is active, angle rotation is suppressed for `stInternalSolid` surfaces:

```cpp
// Read neoweave flags via dynamic option() lookup (no new key in struct needed)
const ConfigOption* opt_en   = print_object_config->option("interlayer_neoweave_enabled");
const ConfigOption* opt_mode = print_object_config->option("interlayer_neoweave_mode");
if (opt_en && opt_en->getBool()) {
    const auto* mode_opt = dynamic_cast<const ConfigOptionEnumGeneric*>(opt_mode);
    if (mode_opt && mode_opt->value == 1 /* Linear */)
        neoweave_lock_angle = true;
}
if (!is_using_template_angle && !neoweave_lock_angle)
    out_angle += this->_layer_angle(...);
```

**Why dynamic `option()` instead of a new key:**  
The `interlayer_neoweave_*` keys are `PrintRegionConfig`, and `FillBase.cpp` only has access to `print_object_config` (`PrintObjectConfig`). Adding a new key to `PrintObjectConfig` requires modifying the struct macro and registering the key in `PrintConfigDef` — any mismatch between `.hpp` and `.cpp` during incremental builds causes `initialize_cache()` to crash on startup. Using dynamic `option()` avoids that risk entirely.

---

## Feature 7 — STL file origin preservation ✅ Validated

### The problem
When importing an STL or 3MF file into OrcaSlicer, the object always appeared centered on the bed, ignoring the file's original XY coordinates. This broke workflows where multiple parts are intentionally positioned relative to each other in the source file.

### The root cause
`ModelObject::rotate()` always calls `center_around_origin()` internally, even when the rotation angle is 0 degrees. The pre-load loop in Plater called `obj->rotate(0, Z)` on every imported object — effectively calling `center_around_origin()` without any user request, destroying the XY position before `load_model_objects()` could capture it.

### The fix
```cpp
// Before: always rotated (and therefore always centered)
obj->rotate(preferred_orientation, Z_axis);

// After: only rotate if there's a real angle to apply
if (std::abs(preferred_orientation) > 1e-9)
    obj->rotate(preferred_orientation, Z_axis);
```

And auto-centering is only applied when the offset is exactly `(0.0, 0.0)` — meaning the object is genuinely at the origin and needs to be placed on the bed.

**Validated cases:** single STL, N STLs without grouping, N STLs grouped, STL whose origin is (0,0) of the model.

---

## Feature 8 — Temporal Link (joint movement groups) ✅ Validated

### What it does
Allows grouping objects to move together in XYZ in the viewport **without affecting slicing**. Useful for parts that have a defined positional relationship (e.g., top and bottom of a shell, a part and its custom support).

Unlike OrcaSlicer's native groups (which affect slicing), Temporal Link is purely a viewport concept — linked objects keep their independent slicing configuration.

### Architecture

**Model core:**
- `int link_group_id { 0 }` in `ModelObject` — the group ID (0 = no group)
- Serialized in cereal for in-memory persistence

**Runtime state (Plater::priv):**
- `m_link_groups` — map of group_id → list of objects
- `m_next_link_group_id` — counter for new groups
- `m_pre_move_offsets` — positions before a move (to calculate deltas)
- `m_syncing_links` — flag to prevent infinite recursion when propagating moves
- `m_floating_z_guard` — defense against the Z=0 snap bug (see Bug 10)

**3MF persistence:**  
The BBS 3MF save functions (`store_bbs_3mf`, `read_from_archive`) are in the BBS source and outside the patch set. The solution is encoding the group as a prefix in the object name: `[L1]Object Name`. On load, `rebuild_link_groups_from_model()` parses the prefixes and reconstructs the groups. Also saved as explicit metadata in the Prusa 3MF format via `3mf.cpp`.

**Re-entrancy Protection (Sync Lock):**  
In session 9, a "UI Cascading Updates" issue was identified where updating a group triggered recursive text calls (wxEVT_TEXT) when rotating or transforming. This was mitigated by injecting a boolean guard `m_syncing_group` directly into `Plater::changed_object` which nips the GUI chain reaction in the bud.

---

## Bug 9 — Startup crash from missing icon ✅ Fixed

`page->new_optgroup(L("Neotko Neoweaving"))` was called without the icon parameter required by the Snapmaker version of OrcaSlicer. Fix: add the correct empty parameter in `Tab.cpp`.

---

## Bug 10 — Floating objects snapped to Z=0 ✅ Fixed

### The problem
Objects legitimately floating above the bed (z > 0) were being dragged to Z=0 when moved in the 3D view.

### The cause
The snap-to-bed code in `GLCanvas3D.cpp` used:
```cpp
if (shift_z != 0.0)  // ← wrong: also "corrected" floating objects
    obj->translate(0, 0, -shift_z);
```

### The fix
```cpp
if (shift_z < 0.0)   // ← correct: only raises objects that are below the bed
    obj->translate(0, 0, -shift_z);
```

This change is applied at 4 locations in `GLCanvas3D.cpp`. The secondary defense `m_floating_z_guard` in Plater uses `wxCallAfter` to detect and revert incorrect snaps that happen outside the direct movement path.

---

## Feature 11 — Temporal Link visual indicators ✅ Validated

In the object list, linked objects display a colored `[L1]`, `[L2]`, etc. prefix according to their group. The palette uses 5 cyclic colors to distinguish different groups. `refresh_link_names()` is called after each link/break/rebuild operation to keep labels current.

---

## Feature 12 — Copy/paste propagates the full group ✅ Validated

When copying an object belonging to a Temporal Link, `Selection::copy_to_clipboard()` expands the selection to include all objects in the same group. On paste, `Plater::regroup_pasted_link_objects()` assigns new group IDs to the copies so the pasted objects form their own independent group.

---

## Feature 13 — Apply Process Preset to selected objects ✅ Validated

### What it does
A button in the top bar of the process panel (ParamsPanel) opens a combo with all **User Presets** (not system or default presets) and applies their values to the selected object as per-object overrides.

### Why only User Presets
System presets can contain values for machine parameters (multi-material, etc.) that don't make sense as per-object overrides. Filtered with `!p.is_system && !p.is_default`.

### Which keys are applied
Only keys valid as per-object overrides: `PrintObjectConfig().keys()` + `PrintRegionConfig().keys()`. Keys from `PrintConfig` (machine-level) are excluded. MM (multi-material) keys are also excluded.

**Technical note:** `Preset::print_options()` returns the full list of keys in a print preset, which is broader than valid per-object override keys. Using `print_options()` directly would try to apply invalid keys as overrides. Always use `PrintObjectConfig().keys() + PrintRegionConfig().keys()`.

---

## Feature 14 — Monotonic Interlayer Nesting ✅ Validated

### What it does
Shifts monotonic top-surface fill lines by half a line spacing perpendicular to the fill direction on alternate layers. Lines from layer N fall into the gaps of layer N-1, improving surface cohesion without changing Z (unlike Neoweaving).

**UI:** Strength > Top/bottom shells > "Monotonic interlayer nesting"

### Activation conditions
- `monotonic_interlayer_fill == true`
- Fill type is `FillMonotonic` (top surface pattern = Monotonic)
- Role is `erTopSolidInfill`
- `layer_id` is odd (alternates between layers)

### Implementation
In `FillBase.cpp`, the fill origin point is shifted half a spacing perpendicular to the fill direction:

```cpp
const coord_t half_sp = coord_t(scale_(this->spacing) * 0.5);
fill_dir.second += Point(
    coord_t(-std::sin(fill_dir.first) * double(half_sp)),
    coord_t( std::cos(fill_dir.first) * double(half_sp)));
```

### Synergy with Neoweaving Linear
Enabling both features produces complete 3D interdigitation:
- Feature 14 shifts lines in XY (half spacing perpendicular)
- Neoweaving Linear alternates them in Z (between nominal and +amplitude)

The result is that each line from layer N is physically surrounded by lines from layer N-1 across all three axes.

---

## Feature 15 — Save Object Config as Preset (session 7)

### What it does
An upload button in the top bar of the ParamsPanel opens a text dialog to enter the new preset name. The system:
1. Clones the currently active print preset as a base
2. Applies the selected object's per-object overrides on top (same safe_keys as Apply)
3. Saves the result as a new detached User Preset
4. Reloads the preset combo to reflect it immediately

### Implementation
`Plater::save_selected_object_config_as_preset(const std::string& preset_name)`:
- `prints.get_edited_preset()` gets the active preset (it's a reference, not a pointer)
- Creates a copy of the preset as `Preset tmp`
- Applies overrides from the selected object's `ModelObject::config`
- `prints.save_current_preset(name, detach=true, save_to_project=false, &tmp, nullptr)`

---

## Feature 16 — Process panel as right-side panel (session 8)

### What it does
Moves the process parameter panel (the fuchsia-marked area in the reference image) from the left sidebar into an independent pane on the right side of the window. The panel can be shown/hidden independently of the sidebar.

### Architecture (wxAuiManager)
OrcaSlicer uses `wxAuiManager` for its layout. Previously, `ParamsPanel` was reparented inside the Sidebar's `wxScrolledWindow`. Now:

1. A `right_container` (wxPanel) is created wrapping `params_panel->get_top_panel()` + the full `ParamsPanel`
2. Added as a `Right` pane to the AUI manager (fixed, not floatable, not movable)
3. `toggle_process_panel()` in `Plater::priv` inverts `m_process_panel_shown` and calls `pane.Show()/Hide()` + `m_aui_mgr.Update()`

### Toggle controls
- **Canvas button** — second item in the existing `collapse_toolbar` (the one containing the sidebar collapse button), using `expand.svg` icon
- **Panel's own button** — `ScalableButton` with `sidebar` icon at the far left of the ParamsPanel top bar. Allows hiding the panel even when the sidebar is collapsed.

**State persistence (Session 9):**
The visible/hidden state of the right side is now properly persisted across sessions using `wxGetApp().app_config->set("process_panel_visible")` from the `Plater` class.

---

## Feature 17 & 18 — Z-Preset Regions & Z-Overrides (Session 9)

### Feature 17: Z-Preset Regions Persistence & GUI
**What it does:** 
Adds an interface for pre-established Z regions that remembers their custom labels ("Zone Label"). Adds the "Cooling & Temp" group with interactive controls directly pulled from the filament system.

### Feature 18: Z-Override GCode Injection (Temperature and Fan Hooks)
**What it does:**  
In `GCode.cpp` (`process_layer`), OrcaSlicer now intelligently injects environmental control GCode commands when crossing a configured preset boundary:
- Real-time monitoring of Z-height block thresholds.
- Immediate emission of temperature commands: `M104` or `M109`.
- Emission of fan configurations: controlling cooling with `M106` and `M107` according to the "Z Override".

---

## Feature status summary

| Feature | Name | Status |
|---------|------|--------|
| 3 | disable_bridge_infill | ✅ Validated |
| 4 | Top/Bottom density UI | ✅ Validated |
| 5 | Neoweaving surface — Wave mode | ✅ Validated (Smoothed Sine Wave) |
| 5 | Neoweaving surface — Linear mode | ⚠️ Implemented, needs hardware test |
| 6 | Neoweaving infill (tristate override) | ✅ Validated |
| 7 | STL origin preservation | ✅ Validated |
| 8 | Temporal Link — core | ✅ Validated (Sync-Lock) |
| Bug 9 | Startup crash missing icon | ✅ Fixed |
| Bug 10 | Floating objects snapped to Z=0 | ✅ Fixed |
| 11 | Temporal Link — visual [Ln] indicators | ✅ Validated |
| 12 | Temporal Link — copy/paste propagates group | ✅ Validated |
| 13 | Apply Process Preset to object | ✅ Validado |
| 14 | Monotonic Interlayer Nesting | ✅ Validated |
| 15 | Save Object Config as Preset | ✅ Implemented |
| 16 | Process panel on right side (toggle) | ✅ Validated (Persistence fixed) |
| 17 | Z-Preset Regions Persistence & GUI | ✅ Validated |
| 18 | Z-Override GCode Injection (Temp / Fan) | ✅ Validated |
| — | Temporal Link — 3MF persistence | ✅ Validated |

---

## Pending work / Known improvements needed

### Neoweaving Wave — Remaining Limitations
The sinusoidal implementation mitigated most of the stuttering, but secondary constraints remain:
1. **Global XY speed cap:** The speed limit is applied to the entire path. It should be calculated per micro-segment.
2. **Pressure advance coordination:** Z trajectory changes during extrusion should coordinate with pressure advance (PA).
3. **Printers without CoreXZ:** On Z-bed printers (Cartesians), this might remain imprecise.

---

## Critical technical notes for future development

### Configuration system
- `interlayer_neoweave_*` and `infill_neoweave_*` keys are `PrintRegionConfig` → accessible in `GCode.cpp` via `m_config` (`FullPrintConfig`, inherits everything)
- The `neoweave_lock_solid_infill_angle` key was removed from the registered keys to avoid `initialize_cache()` crashes. Now implemented via dynamic `option()` in `FillBase.cpp`
- The "Unhandled unknown exception" crash immediately after "Initializing StaticPrintConfigs" **always** means a key declared in the struct macro is not registered in `PrintConfigDef`, or vice versa

### Sidebar and AUI layout
- Sidebar uses `wxAuiManager` (Left pane)
- New process panel uses `wxAuiManager` (Right pane)
- The original sidebar splitters/separators for ParamsPanel are no longer needed
- Use `StaticBox*` not `wxPanel*` for panels with `SetBackgroundColor()`

### Presets and overrides
- `prints.get_edited_preset()` returns a reference, not a pointer (no `get_edited_preset_ptr()` exists)
- `save_current_preset(name, detach=true, ...)` cuts the parent link of the cloned preset
- To filter User Presets only: `!p.is_system && !p.is_default`

### Temporal Link
- `ModelObject::rotate()` **always** calls `center_around_origin()` internally — guard with `if (abs(angle) > 1e-9)`
- Movement synchronization uses the `m_syncing_links` flag to prevent infinite recursion

---

## Build commands

```bash
# Full build (from scratch)
./build_release_macos.sh -x

# Incremental build (reuses compiled deps)
./build_release_macos.sh -s -x
```

The ZIP files are copied over the repository source tree and compiled normally. There are no patching scripts — the ZIP files directly replace the originals in `src/`.

--

**NEOWEAVE INFO FOR USERS**

# OrcaSlicer FullSpectrum - Fork: Neotko ZPreset, Neoweave and Real Per Object Setting+Float objects and link system — Complete Technical Documentation

**Version:** patch series v1–v79 + Notes extra on other files 
**Base:** OrcaSlicer / Snapmaker fork 0.9.3  
**Target platform:** macOS ARM64 (Apple Silicon), builds via `./build_release_macos.sh -s -x`

---

## Table of Contents
1. [Project Overview](#project-overview)
2. [Build Instructions](#build-instructions)
3. [Feature Index](#feature-index)
4. [Z-Override Regions — Deep Dive](#z-override-regions--deep-dive)
5. [Neoweaving](#neoweaving)
6. [Other Features](#other-features)
7. [Architecture Decisions & Lessons Learned](#architecture-decisions--lessons-learned)
8. [File Map](#file-map)
9. [Known Issues & Future Work](#known-issues--future-work)

---

## Project Overview

FullSpectrum Fork: Neotko ZPreset, Neoweave and Real Per Object Setting+Float objects and link system is a set of patches on top of OrcaSlicer adding:

- **Z-Override Regions** — per-height-zone print parameter overrides on a single object
- **Neoweaving** — wave and linear infill weaving modes
- **Temporal Link** — synchronized scale/rotation between linked object instances
- **Simplify3D .factory importer** — v4/v5 format support
- **Right-side process panel** — AUI pane for quick parameter access
- **Various UI improvements** — opacity slider, save-as-preset button, Z-band canvas highlight

---

## Build Instructions

```bash
# Clean build (required after CMakeLists changes or first build)
rm -rf build/arm64
./build_release_macos.sh -s -x

# Incremental build (after .cpp/.hpp changes only)
./build_release_macos.sh -s -x
# or directly:
cd build/arm64 && ninja -j$(sysctl -n hw.ncpu) Snapmaker_Orca
```

**CMakeLists.txt** — new .cpp files must be added here. ZPresetRegions.cpp is already listed.

**Patch application:** extract ZIP into the source root, replacing files, then build.

---

## Feature Index

| Feature | Files | Status |
|---------|-------|--------|
| Z-Override Regions | ZPresetRegions.hpp/cpp, PrintObject.cpp, Print.cpp, Model.hpp | ✅ Complete |
| Prime tower + Z-override compat | Print.cpp, Model.hpp | ✅ Complete |
| Neoweaving Wave | PrintConfig.hpp/cpp, GCode.cpp, FillBase.cpp | ✅ Experimental |
| Neoweaving Linear | PrintConfig.hpp/cpp, GCode.cpp, FillBase.cpp | ✅ (hardware untested) |
| Temporal Link | Model.hpp/cpp, Plater.hpp/cpp, 3mf.cpp | ✅ Complete |
| S3D .factory importer | Format/S3DFactory.hpp/cpp | ✅ Tested |
| Right-side process panel | ParamsPanel.hpp/cpp, Plater.hpp/cpp | ✅ Complete |
| Model opacity slider | ParamsPanel.hpp/cpp | ✅ Complete |
| Save object config as preset | Tab.cpp, Preset.cpp | ✅ Complete |
| Z-band canvas highlight | GLCanvas3D.hpp/cpp | ✅ Complete |
| Z-Override tree nodes | GUI_ObjectList.hpp/cpp, ObjectDataViewModel.hpp/cpp | ✅ Complete |
| Hook 2 (GCode temp/fan injection) | GCode.cpp | ⏳ TODO |

---

## Z-Override Regions — Deep Dive

### Concept

Allows assigning per-zone print parameter overrides to height ranges on a single object.
Example: bottom 10mm at wall_loops=4, top 10mm at sparse_infill_density=10.

This is different from OrcaSlicer's native **Height Range Modifier** (which only modifies
individual volumes) — Z-Override works on the full object cross-section.

### Architecture (v2 — post v75)

**Storage:** `ModelObject::layer_config_ranges` (same map as native height painting).

Each Z-override zone is a `t_layer_height_range → ModelConfig` entry where:
- `ModelConfig` contains `KEY_Z_PRESET_NAME = "z_preset_name"` as an empty-string marker
- Plus any user-checked override keys with their serialized values
- Zones with no user overrides store only the marker key

**Key invariant:** Z ranges are **object-local** (0 = object bottom, h = object top).
Moving the object on the plate does NOT affect ranges.

**Marker key:** `z_preset_name` (value always `""`) — distinguishes our ranges from
native height-painting ranges. Never serialized to GCode; ignored by all slicer paths
except our own UI code.

### Data flow

```
User checks override in dialog
    ↓
save_overrides_from_panel(row)
    → m_rows[row].overrides[key] = serialized_value
    ↓
on_ok() → apply_to_model()
    → for each row: ModelConfig.set(KEY_Z_PRESET_NAME, "")
                    + DynamicPrintConfig tmp; tmp.set_deserialize(key, val, ctx)
                    + ModelConfig.apply(tmp, ignore_nonexistent=true)
    ↓
changed_object(obj_idx) → schedule_background_process()
    ↓
Print::validate() (UI thread, via wxTimer)
    → has_custom_layering check (PATCHED — ignores z_preset_name ranges)
    → layer_height_profile_from_ranges (PATCHED — skips z_preset_name ranges)
    ↓
Print::process() → slicer thread
    → apply_to_print_region_config()
      reads layer_config_ranges, applies per-region configs
      (z_preset_name key is silently skipped — not in PrintRegionConfig)
    ↓ GCode generated with overrides applied per layer
```

### Critical bug history

**Bug 1 — layer_height_profile_from_ranges crash (fixed v73)**
`Print::validate()` calls `update_layer_height_profile()` which calls
`layer_height_profile_from_ranges()`. This function iterates `layer_config_ranges`
and crashes when it encounters configs with unknown keys (our 130+ override keys).
**Fix:** In `PrintObject.cpp::update_layer_height_profile()`, filter the map before
passing to `layer_height_profile_from_ranges()` — only pass ranges that contain
`layer_height`. If none do (our normal case), pass an empty map → uniform profile.

```cpp
// PrintObject.cpp ~line 3835
bool any_lh = false;
for (const auto& [r, cfg] : *ranges_to_use)
    if (cfg.has("layer_height")) { any_lh = true; break; }
static const t_layer_config_ranges s_empty_ranges;
layer_height_profile = layer_height_profile_from_ranges(
    slicing_parameters, any_lh ? lh_ranges : s_empty_ranges);
```

**Bug 2 — Prime tower "same variable layer height" error (fixed v74, v78)**
`has_custom_layering()` in `Model.hpp` returns true if `layer_config_ranges` is
non-empty. Our zones always populate it → prime tower validation fails.

Fix 1 (`Model.hpp`): `has_custom_layering()` only returns true if a range has
`layer_height` key AND does NOT have `z_preset_name`.

Fix 2 (`Print.cpp`): `Print::validate()` has its own inline lambda replicating
`has_custom_layering()` — this also needed the same fix.

```cpp
// Model.hpp
bool has_custom_layering() const {
    if (!this->layer_height_profile.empty()) return true;
    for (const auto& [range, cfg] : this->layer_config_ranges) {
        if (cfg.has("z_preset_name")) continue; // our marker — skip
        if (cfg.has("layer_height"))  return true;
    }
    return false;
}

// Print.cpp — same logic inline in validate()
```

### UI Structure

```
ZPresetRegionsDialog (wxDialog, 1020×560, resizable)
├── Header label
├── Body (wxHORIZONTAL)
│   ├── LEFT: wxGrid (3 cols: Z min | Z max | Label)
│   │         + "Add Zone" / "Remove Zone" buttons
│   ├── CENTRE: wxScrolledWindow (override panel)
│   │   ├── Hint text (hidden when zone selected)
│   │   └── Per-group sections:
│   │       ├── Bold colored group header
│   │       └── wxFlexGridSizer (3 cols: [✓] | label | value ctrl)
│   │           value ctrl = wxChoice (enum/bool) or wxTextCtrl (numeric/string)
│   └── RIGHT: ZRegionPreview (160px wide, draggable boundaries)
└── Bottom: Cancel | Apply buttons
```

### Zone management rules (v78)

- **Open dialog:** zones always span [0, obj_height]. Front zone clamped to z_min=0,
  back zone clamped to z_max=obj_height.
- **Add zone:** splits the currently selected zone (or last zone) at its midpoint.
  New zone inherits parent's overrides as starting point.
- **Remove zone:** expands adjacent zone to fill gap. If last zone: confirms and
  clears all z-override ranges from ModelObject (disables feature entirely).
- **Draggable boundaries:** ZRegionPreview handles mouse drag, updates both m_rows
  and grid cells, fires highlight_band_in_canvas.
- **Row select → load:** saves current panel to m_rows[prev], loads m_rows[next].
- **Apply:** saves panel → validates → writes to ModelObject → changed_object().

### Override panel — supported types

| Config type | UI control | Value stored |
|------------|-----------|-------------|
| `coBool` | wxChoice ("false"/"true") | "true" or "false" |
| `coEnum` (listed below) | wxChoice (enum strings) | serialized enum string |
| `coFloat`, `coInt`, `coFloatOrPercent` | wxTextCtrl | plain number or "50%" |
| `coString` | wxTextCtrl | raw string |

**Keys with wxChoice (enum dropdowns):**

| Key | Choices |
|-----|---------|
| `seam_position` | nearest, aligned, aligned_back, back, random |
| `wall_sequence` | inner wall/outer wall, outer wall/inner wall, inner-outer-inner wall |
| `wall_direction` | auto, ccw, cw |
| `ironing_type` | no ironing, top, topmost, solid |
| `fuzzy_skin` | none, external, all, allwalls |
| `sparse_infill_pattern` | monotonic, monotonicline, rectilinear, … (27 options) |
| `top_surface_pattern` | monotonic, monotonicline, rectilinear, … (10 options) |
| `bottom_surface_pattern` | same as top_surface_pattern |

### Key groups in override panel

1. **Walls** — wall_loops, wall_sequence, wall_direction, line widths, speeds, flow, single-wall options
2. **Seam** — seam_position, seam_gap, wipe_on_loops, wipe_before_external_loop, wipe_speed
3. **Infill** — density, pattern, line width, speed, filament, rotate template, combination, anchors, overlap
4. **Top / Bottom** — shell layers, thickness, patterns, speeds, density, flow ratio, overlap
5. **Ironing** — type, speed, flow, spacing, inset
6. **Speed & Bridges** — bridge speed, gap fill, small perimeter, overhang speed
7. **Flow** — print_flow_ratio, bridge_flow
8. **Fuzzy Skin** — type, thickness, point distance
9. **Layer Height** — layer_height (use with caution — triggers custom layering checks)
10. **Advanced** — filter_out_gap_fill, gap_closing_radius, enable_arc_fitting

### ZRegionPreview widget

Custom `wxPanel` (160px wide). Coordinate system: Z=0 at bottom → high Y; Z=max → low Y.

- Colored bands per zone (palette of 7 colors, cycles)
- Selected band: lightened 140%
- Draggable boundaries: hit test ±6px, cursor changes to wxCURSOR_SIZENS
- Shows override count per zone (number inside band)
- Tick marks at all boundaries with Z value labels
- Fires `m_on_boundary_drag(boundary_idx, new_z)` callback on drag

### 3D canvas Z-band highlight (GLCanvas3D)

```cpp
// Highlight a Z range in world coordinates on the canvas:
canvas->set_z_band_highlight(z_min_world, z_max_world, x_min, y_min, x_max, y_max);
canvas->clear_z_band_highlight();
```

Rendered as two semi-transparent planes (40% alpha blue border, 8% alpha fill).
Cleared on: dialog close, Cancel, new project, `priv::reset()`.

---

## Neoweaving

Wave-pattern and linear infill modes. Adds keys:
- `neoweave_mode` (enum: none / wave / linear)
- `neoweave_amplitude`, `neoweave_frequency`, `neoweave_angle_lock`

Implemented in `FillBase.cpp` (infill geometry generation) and `GCode.cpp` (speed modulation).
Marked as Experimental in UI. Linear mode: hardware test pending.

---

## Other Features

### Temporal Link
Linked object instances (Ctrl+drag creates link) propagate rotation and scale changes.
Prevents "floating Z" when a linked object is rotated (Z is recalculated from world bbox).
Data stored in `ModelObject::linked_ids` (custom field in Model.hpp/cpp + 3MF persistence).

### Simplify3D .factory Importer
`Format/S3DFactory.hpp/cpp` — parses v4 and v5 .factory XML files.
Imports: models, process profiles, support settings, layer heights, colors.
Registered in `Format/` CMakeLists and accessible via File → Import.

### Right-Side Process Panel
AUI pane on right edge of main window. Toggle via: right-edge GLToolbar button OR
"Process" button in ParamsPanel top bar. Visibility persistence via app_config: TODO.

### Model Opacity Slider
In ParamsPanel. Adjusts `GLVolume::color.a()` for the selected object. Range 10–100%.

---

## Architecture Decisions & Lessons Learned

### 1. ModelConfig API
```cpp
cfg.option(key)          // get ConfigOption* (NOT optptr — that doesn't exist on ModelConfig)
cfg.set(key, value)      // set typed value
cfg.set_key_value(key, ConfigOption*)  // takes ownership of raw pointer
cfg.apply(DynamicPrintConfig, ignore_nonexistent)
cfg.has(key)             // check presence
cfg.opt_serialize(key)   // returns raw string WITHOUT surrounding quotes

// set_deserialize requires ConfigSubstitutionContext:
ConfigSubstitutionContext ctx(ForwardCompatibilitySubstitutionRule::Disable);
cfg.set_deserialize(key, val, ctx);
```

### 2. Enum value `comDevelop`
The correct enum value for developer mode is `comDevelop` (NOT `comNone`, `comDeveloper`).
Used when adding new keys to `PrintConfig` to avoid validation errors.

### 3. Lambda capture in TBB parallel_for
TBB lambdas with explicit capture lists `[this, x]` cannot implicitly capture outer
variables. Use file-scope structs with static methods instead of `auto lambda = [&]{}`.

### 4. `has_custom_layering()` is checked in TWO places
Both `Model.hpp::has_custom_layering()` AND an inline lambda in `Print.cpp::validate()`
replicate the same logic. Any patch must update BOTH.

### 5. `layer_height_profile_from_ranges` expects only `layer_height` ranges
This function (in the base Orca codebase, not in our patches) assumes every
`ModelConfig` entry in the map has a `layer_height` key. Passing configs with
arbitrary keys causes out-of-range crashes. Always filter before calling.

### 6. Z-band highlight must be cleared in all exit paths
The 3D canvas highlight persists across frames. Must call `clear_z_band_highlight()`
in: wxEVT_CLOSE_WINDOW, wxID_CANCEL button, and any path that closes the dialog.

### 7. changed_object() must NOT be called from inside apply_to_model()
Calling `changed_object()` triggers re-entrant UI updates while the dialog is alive.
Always call it AFTER `ShowModal()` returns, in the entry-point function.

### 8. Signal for crash diagnosis
The crash at `layer_height_profile_from_ranges` showed as:
```
[WARNING in_range_cast.h:38] value -634136515 out of range
zsh: segmentation fault
```
The `in_range_cast` warning is a Chromium base library warning about integer overflow —
it's a symptom, not the cause. Actual cause was memory corruption from unexpected
config keys. Use signal handlers + `backtrace_symbols()` for crash diagnosis.

---

## File Map

```
src/
├── libslic3r/
│   ├── PrintConfig.hpp      Fork: Neotko ZPreset, Neoweave and Real Per Object Setting+Float objects and link system keys: z_preset_name (comDevelop),
│   │                        neoweave_*, all new feature config keys
│   ├── PrintConfig.cpp      Enum string maps, key defaults, group assignments
│   ├── PrintObject.cpp      apply_to_print_region_config (debug removed v76),
│   │                        update_layer_height_profile (layer_height filter v73),
│   │                        apply_to_print_region_config (cleanup v76)
│   ├── Print.cpp            validate() patches:
│   │                          - has_custom_layering inline lambda (v78)
│   │                          - prime tower variable layer height check (v78)
│   ├── Model.hpp            has_custom_layering() (v74+v78),
│   │                        ModelObject::linked_ids (Temporal Link)
│   ├── Model.cpp            Temporal Link propagation
│   ├── GCode.cpp            Neoweaving speed modulation, layer-change hooks
│   ├── Fill/FillBase.cpp    Neoweaving geometry (wave + linear modes)
│   └── Format/
│       ├── 3mf.cpp          Temporal Link persistence
│       └── S3DFactory.hpp/cpp  Simplify3D .factory importer
└── slic3r/GUI/
    ├── ZPresetRegions.hpp   ZRegionPreview widget, ZPresetRegionsDialog class
    ├── ZPresetRegions.cpp   Full implementation (639 lines as of v78)
    ├── Plater.hpp/cpp       open_z_preset_regions_for_selection(), menu injection,
    │                        changed_object() (debug removed v76)
    ├── GLCanvas3D.hpp/cpp   ZBandHighlight struct, render(), set/clear methods
    ├── GUI_ObjectList.hpp/cpp  Z-override tree nodes (itZPresetRoot/itZPreset),
    │                           right-click menu injection
    ├── ObjectDataViewModel.hpp/cpp  itZPreset node types
    ├── Tab.cpp              Save-as-preset button, neoweave UI
    ├── Preset.cpp           Neoweave preset handling
    ├── ParamsPanel.hpp/cpp  Right-side panel, opacity slider, z_regions_btn
    └── Selection.hpp/cpp    Feature 12 (selection improvements)
```

---

## Known Issues & Future Work

### TODO: Hook 2 — GCode temperature/fan injection
At each layer change in GCode.cpp, detect which z-override zone covers the current Z,
and emit M-codes for:
- `nozzle_temperature` → M104/M109
- `bed_temperature` → M140/M190  
- `fan_speed` → M106

Track last-emitted values to avoid redundant codes. Implementation point:
`GCode.cpp::change_layer()` or `process_layer()`.

### TODO: Right panel visibility persistence
Save/restore the right-side process panel visibility in `app_config`.

### TODO: Neoweaving Linear hardware test
Linear mode is implemented but not validated on physical hardware.

### TODO: 3MF round-trip for Z-override zones
`layer_config_ranges` is already serialized to 3MF by the base Orca code.
However, the `z_preset_name` marker key needs to survive the round-trip.
Verify in `Format/3mf.cpp` that arbitrary string keys in ModelConfig are preserved.

### KNOWN: Zone label not persisted
Zone labels (user-editable names) are NOT stored in ModelConfig — only the override
keys are stored. On re-open, labels revert to "Zone N". Fix: store label as a
specially-prefixed string key in ModelConfig, e.g. `_zpreset_label`.

### KNOWN: Preview band label shows count, not name
The ZRegionPreview shows override count (e.g. "3") instead of zone label.
Once label persistence is fixed, display label text instead.

### KNOWN: Z-band canvas highlight disappears on scene reload
`reload_scene()` clears some GLCanvas state. The ZBandHighlight may need to be
re-applied after reload. Tracked from session 13.

IMPORTANT

Much of the to-do and bugs have been fixed, check read implementation_plan.md and task.md for the full info. Used Antigravity to help with bug fixes and almost all to-do is Done
